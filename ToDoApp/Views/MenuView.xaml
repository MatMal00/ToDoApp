<Page x:Class="ToDoApp.MenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ToDoApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MenuView">

    <Grid Background="White">
        <Grid.CommandBindings>
            <CommandBinding Command="{Binding HandlePageChange}"
        </Grid.CommandBindings>
        <WrapPanel Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center">
            <local:NavButton Text="Tasks ToDo" NavUri="/Views/TasksToDoView.xaml" />
            <local:NavButton Text="Done Tasks" NavUri="/Views/DoneTasksView.xaml" />
        </WrapPanel>
    </Grid>
</Page>
